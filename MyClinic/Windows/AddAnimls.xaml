<Window x:Class="MyClinic.Windows.AddAnimls"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MyClinic.Windows"
        mc:Ignorable="d"
        Title="AddAnimls" Height="450" Width="800">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Кличка"
                   FontSize="18"
                   HorizontalAlignment="Left"
                   Margin="20 0 0 0"
                   VerticalAlignment="Center"
                   Grid.ColumnSpan="3"/>
        <TextBox Grid.Row="1"
                 Grid.ColumnSpan="2"
                 VerticalAlignment="Top"
                 HorizontalAlignment="Left"
                 Margin="20 0 0 0"
                 Name="TitleTb"
                 Height="35"
                 FontSize="16"
                 VerticalContentAlignment="Center"
                 Width="200"/>
        <TextBlock Text="Порода"
                   FontSize="18"
                   HorizontalAlignment="Left"
                   Margin="20 0 0 0"
                   VerticalAlignment="Center"
                   Grid.Row="2"
                   Grid.ColumnSpan="3"/>
        <ComboBox Grid.Row="3"
                  Grid.ColumnSpan="2"
                  Width="200"
                  FontSize="16"
                  VerticalAlignment="Top"
                  HorizontalAlignment="Left"
                  Margin="20 0 0 0"
                  Height="40"
                  ItemsSource="{Binding Animals}"
                  Name="AuthorCm">
            <ComboBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel>
                        <TextBlock Text="{Binding Gender.Name}"/>
                    </StackPanel>
                </DataTemplate>
            </ComboBox.ItemTemplate>
        </ComboBox>

        <!-- Кнопки -->
        <StackPanel Grid.Row="13" Orientation="Horizontal" HorizontalAlignment="Right">
           
            <Button Content="Сохранить" Width="80"/>
        </StackPanel>
    </Grid>
</Window>
